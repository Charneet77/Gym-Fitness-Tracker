function goBack() {
    window.history.back();
}

function viewDetails(trainerName) {
    alert("Viewing details for: " + trainerName);
    // Redirect to trainer details page or display additional info
}
